[project]
name = "ecommerce-scraper"
version = "0.1.0"
description = "Web scraper for e-commerce static test site using Beautiful Soup"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "pytest>=9.0.2",
]
